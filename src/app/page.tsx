import NavBar from "@/components/NavBar";
import Image from "next/image";
import React, { useState } from "react";

const page = () => {
  return (
    <main>
      <NavBar />
    </main>
  );
};

export default page;
